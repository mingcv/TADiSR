# Third-Party Components

`ppocr/` is the lightweight OCR implementation used by the original TADiSR
training losses and evaluation. It is not required for public CogView4
inference. OCR model archives are intentionally excluded; obtain compatible
weights from the upstream PP-OCR project before running training.
